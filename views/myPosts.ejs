<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Posts</title>
    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="/css/blog-style.css" />
    <style>
      .delete-btn {
        background: #ff4d4d;
        border: none;
        padding: 5px 10px;
        color: white;
        border-radius: 5px;
        cursor: pointer;
        margin-left: 5px;
        font-size: 0.9rem;
      }

      .delete-btn:hover {
        background: #e60000;
      }
    </style>
  </head>

  <body>
    <%- include('partials/header') %>

    <div style="text-align: right; margin: 1rem">
      <a href="/create-post" class="btn-primary">➕ New Post</a>
    </div>
    <main class="blog-container">
      <% if (blogs.length===0) { %>
      <p style="text-align: center; padding: 2rem">
        You haven't posted anything yet.
      </p>
      <% } else { %> <% blogs.forEach(blog=> { %>
      <div class="blog-card">
        <h2><%= blog.title %></h2>

        <p class="meta">
          <span class="author"> <%= blog.author %> </span>
          &nbsp;•&nbsp;
          <span class="date">
            <%= blog.created_at.toLocaleDateString("en-US", { month: "long" ,
            day: "numeric" , year: "numeric" }) %>
          </span>
        </p>

        <p><%= blog.content.substring(0, 150) %>...</p>

        <div class="blog-actions">
          <a href="/blog/<%= blog.id %>?returnTo" class="read-more">Read More</a>
          <button
            class="like-btn <%= blog.userLiked ? 'active' : '' %>"
            data-blog-id="<%= blog.id %>"
          >
            <span class="heart"> ❤️ </span>
            <span class="count"> <%= blog.likeCount %> </span>
          </button>
          <form action="/delete-post/<%= blog.id %>" method="POST" style="display: inline;">
            <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete the blog?')">🗑</button>
          </form>
        </div>
      </div>
      <% }) %> <% } %>
    </main>

    <%- include('partials/footer') %>
    <script src="/js/blog-actions.js"></script>
  </body>
</html>
