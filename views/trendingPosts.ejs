<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/blog-style.css">
</head>
<body>
    <%- include('partials/header') %>

    <main class="blog-container">
        <% blogs.forEach(blog => { %>
            <div class="blog-card">
                <h2><%= blog.title %></h2>

                <p class="meta">
                    <span class="author"><%= blog.author %></span>
                    &nbsp;•&nbsp;
                    <span class="date">
                        <%= blog.created_at.toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" }) %>
                    </span>
                </p>

                <p><%= blog.content.substring(0, 150) %>...</p>

                <div class="blog-actions">
                    <a href="/blog/<%= blog.id %>?returnTo=home" class="read-more">Read More</a>
                    <button class="like-btn <%= blog.userLiked ? 'active' : '' %>" data-blog-id="<%= blog.id %>">
                        <span class="heart">❤️</span>
                        <span class="count"><%= blog.likeCount %></span>
                    </button>
                </div>
            </div>
        <% }) %>
    </main>

    <%- include('partials/footer') %>
    <script src="/js/blog-actions.js"></script>
</body>
</html>